<%#
# Partial "users/user"
# Called by: users#index
%>

<% if user_counter && user_counter.zero? %>
  <tr>
    <th>Name</th><th>Email</th>
  </tr>
<%  end %>

<tr>
  <td>
    <%= gravatar_for user, :size => 30 %>
    <%= link_to user.name, user %>
  </td>
  <td>
    <%= user.email %>
  </td>
  <% if current_user.admin? %>
    <td>
      <%= link_to "Modify", admin_user_path(user) %>
    </td>
  <% end %>
</tr>
